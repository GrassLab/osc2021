#ifndef UTIL_H
#define UTIL_H

int strlen(char *s); // calculate string length
int strcmp(char *s1, char *s2); // compare two string is equal ?
int atoi(char* input); // string to int
long long hex2int(char* hex); // hex string to integer

#endif